[build]
  command = "pnpm install && pnpm build"
  publish = "dist/public"
  functions = "dist/functions"

[dev]
  command = "pnpm dev"
  port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
